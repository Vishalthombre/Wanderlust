Installation 
open the folder in a visual studio code 
in terminal run cmd **npm install** it will install all the npm packages 
to start hosting run cmd **node app.js** and then use this path on any web browser 
**http://localhost:8080/listings**
